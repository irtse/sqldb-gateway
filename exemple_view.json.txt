{
  "data": [
    {
      "actions": [ // Actions define any action button accessible from view.
        {
          "description": "no description...", // hover description
          "is_view": false, // define if we are redirecting on a view 
          "link_path": "/dbtask?rawview=enable&id=%id%", // link_path is the url to call when action activate (in case of %%, should replace with parameters)
          "method": "PUT", // method to employed on call
          "name": "save task", // name of action button
          "parameters": [ // parameter to modified
            "id"
          ],
          "schema": { // obj body (if POST/PUT) && response format
            "comment": { // field and its description
              "label": "comment", // ui name
              "type": "text", // type expected
              "index": 1, 
              "description": "no description...", 
              "placeholder": "",
              "default_value": null, // if got a default if no value
              "required": false, // if needed in a body (POST method)
              "readonly": false,
              "values_path": ""
            },
          },
          "schema_name": "dbschema_id" // schema name where the scheme is based on
        }
      ],
      "category": null, // view category it describe a sub organized folder in the menu
      "description": "no description...", // hover description of the view
      "index": 1, // where its positionned in the view menu
      "is_list": false, // if view content implied a list of item
      "items": [ // list of items implied in the view
        {
          "data_path": "/generic/dbuser?rows=1", // path to GET content inside item (not used in a case of list)
          "link_path": "", // path to REDIRECT on (not used in a case of not list)
          "values": { // current values of field of the item
            "comment": "fulfill user god",
          },
          "values_path": {} // any path to fill values of a external field of the item
        },
      ],
      "link_path": "/generic/dbview?rows=1", // path to REDIRECT on at view level (same as view)
      "name": "View List Task Assignee", // name of the view 
      "schema": { // struct engaged into mapping items
        "comment": {
          "default_value": null, // default value if nil
          "description": "no description...", // hover description
          "index": 1,
          "label": "comment",  // ui name
          "values_path": "",  // path to reach content if a select
          "placeholder": "", // what's in placeholder
          "readonly": false, // if its a readonly field (no input just text field)
          "required": false, // if needed on POST body
          "type": "text" // type involved (DB type for now)
        },
      },
      "schema_name": "dbtask"
    },
  ],
  "error": null
}